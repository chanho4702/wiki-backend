-- 변경 요약(W22) — "무엇을 왜 고쳤는지"를 리비전에 남긴다.
--
-- 지금까지 이력에는 누가·언제만 있었다. 버전이 수십 개가 되면 목록을 훑어도 어느 것이
-- 되돌릴 지점인지 알 수 없어, 결국 본문을 하나씩 열어 비교해야 했다.
--
-- 컨플루언스와 같이 **선택 입력**이다. 강제하면 대부분 "수정"만 적힌 이력이 쌓여 오히려
-- 신호가 죽는다. 비어 있으면 화면이 아무것도 그리지 않는다(빈 문자열로 저장하지 않는다).
ALTER TABLE page_revision
    ADD COLUMN change_note VARCHAR(500);
